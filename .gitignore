# Python
.venv/
__pycache__/
*.pyc

# Daten (persönliche Texte / API-Keys / Metadaten)
corpus.json
summaries.json
articles.db
seen.db
llm_log.db
profile.json
profile.md
*.key
.env
.enrichment_cache/
.backfill_cache/
.openalex_cache/
.own_refs_cache/
.escalation_cache/
journal_profiles.json
.agent_context.json
.agent_context.txt

# MOJO 2.0 Refs-Pipeline (lokale DB + Cache + Snapshot-Daten)
own_refs.db
bezugsautoren.db
label_exclusions.json
ranker_params.json
own_refs_corpus_freq.json
trigger_refs_index.json
adversarial_corpus_freq.json
backtest_data/
output/own_refs_index.json
output/*.md

# Digest-Run-Logs (lokales Lauf-Tracking, kein Repo-Inhalt)
output/digest_*.log
output/web_*.log

# §2.5-Eskalations-Pilot-Outputs (per-Run-Artefakte, kein Repo-Inhalt)
output/escalation_pilot_*.json
output/pilot_wrong_les_*.json

# OS
.DS_Store

# Obsolet (aus Phase 1, inzwischen gelöscht)
config.toml

# Temporäre Dateien
*.tmp
/tmp/
backups/
exports/

# launchd (nur lokal, nie ins Repo)
launchd/*.log
launchd/*.plist
!launchd/*.plist.template

# Über die Oberfläche hereingegebene eigene Texte (Manuskripte, Entwürfe)
eigene_texte/
output/textimport.log
