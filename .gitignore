# Python
.venv/
__pycache__/
*.pyc

# Daten (persönliche Texte / API-Keys / Metadaten)
corpus.json
summaries.json
articles.db
seen.db
llm_log.db
profile.json
profile.md
*.key
.env
.enrichment_cache/
.backfill_cache/
.openalex_cache/
journal_profiles.json
.agent_context.json
.agent_context.txt

# Digest-Run-Logs (lokales Lauf-Tracking, kein Repo-Inhalt)
output/digest_*.log
output/web_*.log

# OS
.DS_Store

# Obsolet (aus Phase 1, inzwischen gelöscht)
config.toml

# Temporäre Dateien
*.tmp
/tmp/
backups/
exports/

# launchd (nur lokal, nie ins Repo)
launchd/*.log
launchd/*.plist
!launchd/*.plist.template
